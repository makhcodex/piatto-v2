"""Product and category management. TODO: fill in during implementation."""

from aiogram import Router

router = Router(name="admin.catalogue")

# Handlers to implement: add/edit/delete product, add/delete category,
# toggle in_stock, set max_quantity. All via product_service / category_service.
